<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.six.hats.brand">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JullayBrandApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".ServicesViewpagerActivity"/>
        <activity android:name=".StaffViewpagerActivity"/>

        <activity
            android:name=".BizGallery"
            android:label="@string/gallery" />
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".Feedbacks"
            android:label="@string/feedbacks" />
        <activity android:name=".AboutUsActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.JullayBrandApp.NoActionBar" />
        <activity
            android:name=".Splash"
            android:theme="@style/Theme.JullayBrandApp.NoAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".register.RegistrationActivity" />
    </application>

</manifest>